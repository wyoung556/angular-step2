{
  "name": "angular-step2",
  "version": "1.0.0",
  "description": "An Angular app that uses a model and controller",
  "scripts": {
    "start": "http-server -o -a localhost -d false -p 8000 -c-1 ./app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wayland Young",
  "license": "MIT",
  "dependencies": {
    "angular": "^1.5.8",
    "bootstrap": "^3.3.7"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-jshint": "^2.0.1",
    "gulp-sourcemaps": "^2.1.1",
    "gulp-uglify": "^2.0.0",
    "http-server": "^0.9.0",
    "jshint": "^2.9.3"
  }
}
